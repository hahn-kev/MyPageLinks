{
  "name": "my-page-links",
  "version": "1.0.0",
  "description": "A Firefox extension to manage and organize your page links.",
  "private": true,
  "scripts": {
    "build": "tsc && pnpm run copy-assets",
    "copy-assets": "node scripts/copy-assets.js",
    "dev": "pnpm run build && web-ext run --source-dir dist",
    "lint": "web-ext lint --source-dir dist",
    "package": "pnpm run build && web-ext build --source-dir dist --artifacts-dir artifacts"
  },
  "keywords": ["firefox", "extension", "webextension"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/firefox-webext-browser": "^143.0.0",
    "typescript": "^7.0.2",
    "web-ext": "^10.4.0"
  }
}
